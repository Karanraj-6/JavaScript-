//* ONLY WORKS IN BROWSER WINDOW

window.alert("hi")  //  shows alert box.

window.setTimeout(fn, 1000) // calls a function after delay.

window.setInterval(fn, 1000) // calls function repeatedly.

window.confirm("are you naughty?") // returns true/false.

window.prompt("type something") // asks input.